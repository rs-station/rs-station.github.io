title: Reciprocal Space Station

# Build posts with a future date (e.g. the June 25 announcement) so they
# appear in PR previews and on the live site without waiting for the date.
future: true

plugins:
  - jekyll-font-awesome-sass

# NOTE: you can ignore this `port` line and any changes to it;
# It is only relevant for local builds and has no effect on GitHub
port: 4504
